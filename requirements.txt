# Neonveil — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Optional (for MusicGen integration — requires CUDA GPU):
#   pip install torch audiocraft

# Required
numpy>=1.24
pyyaml>=6.0

# Testing
pytest>=7.0
